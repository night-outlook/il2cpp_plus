#pragma once
// Versioned default for reproducible Player builds. Unit tests may explicitly
// override the effective diagnostics macro; correctness guards are unaffected.
#define HYBRIDCLR_R02_DEFAULT_DIAGNOSTICS_LEVEL 2
